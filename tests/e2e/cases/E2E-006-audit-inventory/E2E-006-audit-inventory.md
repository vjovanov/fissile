# E2E-006-audit-inventory: audit reports overflows plus optional inventory sections

`fissile audit` reports the same standing findings as `check` and, when their
flags are passed, the largest files per unit, stale exceptions, and rule-coverage
gaps. It is the whole-repo inventory and migration surface
(§FS-004-check-audit.2).
