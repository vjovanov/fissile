args = ["check", "--staged", "--no-color"]
exit = 0
git = true
# `src/moved.rs` is absent and nothing staged its removal: `stale = "error"` must
# not turn that into a blocked commit.
stdout_equals = "ok"
