args = ["check", "--no-color"]
exit = 0
stdout_equals = """
soft: 1 file over the 2-line budget [rule: rust, message: m]
  Split the file.
    src/mid.rs: 3 non-blank lines (budget 2)

hint: fissile measure <path>... reports size and headroom for the files you split into."""
