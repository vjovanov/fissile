args = ["check", "--no-color"]
exit = 1
stdout_equals = """
hard: 1 file over the 200-byte budget [rule: blob, message: m]
  Split the file.
    src/data.bin: 250 bytes (an exception here would accept 300)

hint: fissile measure <path>... reports size and headroom for the files you split into."""
